# Upgraded requirements.txt
# Generated with safe upgrades while keeping ES 6.6.0 compatibility

# ============================================================================
# LOCKED - Must stay at these versions for Elasticsearch 6.6.0 compatibility
# ============================================================================
elasticsearch==6.3.1
elasticsearch-dsl==6.2.1

# ============================================================================
# CORE FLASK STACK - Upgraded to latest compatible versions
# ============================================================================
Flask==3.0.0
Jinja2==3.1.4
Werkzeug==3.0.3
itsdangerous==2.1.2
click==8.1.7

# ============================================================================
# HTTP/REQUESTS STACK - Upgraded to latest
# ============================================================================
# Note: Dependencies (urllib3, certifi, chardet, idna) will be installed automatically
requests==2.32.3

# ============================================================================
# UTILITIES
# ============================================================================
python-dateutil==2.9.0
pyquery==1.4.3  # Keep at 1.4.3 (2.0.0 may have breaking changes)
humanize==3.14.0

# ============================================================================
# OAUTH - Upgraded
# ============================================================================
requests-oauthlib==1.3.1
oauthlib==3.2.2

# ============================================================================
# OTHER PACKAGES
# ============================================================================
# Note: Dependencies (future) will be installed automatically
PyTumblr==0.0.8  # Check if maintained, may need fork

# ============================================================================
# DEVELOPMENT TOOLS (Optional - can upgrade freely)
# ============================================================================
# Note: Dependencies (astroid, mccabe, lazy-object-proxy, wrapt, typed-ast, pycodestyle)
# will be installed automatically
autopep8==2.0.4
isort==5.13.2
pylint==3.2.3

