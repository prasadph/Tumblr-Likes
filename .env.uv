# UV Configuration for External SSD
# This moves the cache to the same filesystem to enable hardlinking and save space

# Set cache directory to project directory (same filesystem)
export UV_CACHE_DIR="$(pwd)/.uv-cache"

# Or use symlinks (alternative)
# export UV_LINK_MODE=symlink
